/**
 * Scanner Module — Public API
 *
 * All cross-module imports must go through this barrel.
 * Never import directly from internal files.
 *
 * Sub-modules: message, url, qr, screenshot
 *
 * Exports will be added as scanner features are implemented:
 * - Scan actions (message, URL, QR, screenshot)
 * - Scan result types
 * - Scan history queries
 */

// export { } from './actions/...';
// export type { } from './types/...';
