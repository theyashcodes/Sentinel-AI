'use client';

import type { ReactNode } from 'react';

/**
 * Query Provider (Stub)
 *
 * TanStack Query provider for server state management.
 * Will be configured when data-fetching features are implemented.
 */
export function QueryProvider({ children }: { children: ReactNode }) {
  // TanStack Query will be initialized here in a future phase
  return <>{children}</>;
}
